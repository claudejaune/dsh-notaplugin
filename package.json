{
  "name": "dsh-notaplugin",
  "private": true,
  "version": "0.0.0",
  "description": "A collection of small, installable plugins for the DeepSeek Harness (dsh) web GUI.",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck"
  }
}
